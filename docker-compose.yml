version: '3.8'

services:
  frontend:
    build: ./frontend/video-stream
    ports:
      - "80:80"
    restart: always
    depends_on:
      - backend

  backend:
    build: ./server
    ports:
      - "3000:3000"
    restart: always
    environment:
      - NODE_ENV=production
